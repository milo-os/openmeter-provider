apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/name: controller-template
    app.kubernetes.io/managed-by: kustomize
  name: controller-template-webhook
spec:
  ports:
    - port: 443
      protocol: TCP
      targetPort: 9443
  selector:
    control-plane: controller-template
    app.kubernetes.io/name: controller-template
