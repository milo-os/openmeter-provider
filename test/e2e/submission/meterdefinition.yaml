apiVersion: billing.miloapis.com/v1alpha1
kind: MeterDefinition
metadata:
  name: e2e-submission-requests
spec:
  # No '-' in the path segments so openmeter.MeterSlug's '.'/'/' -> '_'
  # mapping yields a slug OpenMeter's `^[a-z0-9]+(?:_[a-z0-9]+)*$` accepts.
  meterName: e2e.miloapis.com/submission/requests
  phase: Published
  displayName: Submission Consumer E2E Requests
  measurement:
    aggregation: Sum
    unit: "{request}"
  billing:
    consumedUnit: "{request}"
    pricingUnit: "{request}"
  monitoredResourceTypes:
    - ri2.miloapis.com/hosted-api
